## Current task

The plugin-craft seed is complete: four skills under `skills/`, an enforcement script wired into `just precommit`, and the eight ddaanet source facts retired or reduced so the tier index is back under the loader cutoff.

One thread stays live and is not this repo's to drive: the sibling extraction passes for `craft` and `shell-scripting` run in parallel against the same ddaanet memory tier. `hook-output-channels` was reduced here to its section 7, which is `craft`'s source, and eleven inbound pointers in facts that stay were repointed at the new skills. If a parallel pass wrote the same files, that surfaces as a merge on the memory store rather than as silent loss.